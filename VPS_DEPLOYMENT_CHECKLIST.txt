═══════════════════════════════════════════════════════════════
    מערכת ניהול גיוס - רשימת קבצים לפריסה ב-VPS
    Recruitment Management System - VPS Deployment Files
═══════════════════════════════════════════════════════════════

📦 קבצים שיצרתי עבורך:
────────────────────────────────────

1. database_vps_deployment_backup.sql
   📊 גיבוי מלא של מסד הנתונים
   - גודל: 6.6 MB
   - שורות: 12,922
   - כולל: כל הטבלאות, נתונים, אינדקסים ואילוצים
   - פורמט: PostgreSQL SQL dump
   
2. VPS_DEPLOYMENT_GUIDE.md
   📖 מדריך פריסה מלא בעברית
   - הוראות התקנה שלב אחר שלב
   - הגדרת PostgreSQL
   - הגדרת Node.js + PM2
   - הגדרת Nginx + SSL
   - אבטחה וגיבויים
   - פתרון בעיות

3. VPS_DEPLOYMENT_CHECKLIST.txt
   ✅ הקובץ הזה - רשימת קבצים וחומרים


📂 קבצים נוספים שצריך להעתיק ל-VPS:
────────────────────────────────────────

הקוד המלא של הפרויקט כולל:

├── server/              → קוד Backend
│   ├── index.ts
│   ├── routes.ts
│   ├── storage.ts
│   ├── auth.ts
│   └── ...
├── client/              → קוד Frontend
│   └── src/
│       ├── pages/
│       ├── components/
│       └── ...
├── shared/              → קוד משותף
│   └── schema.ts
├── package.json         → תלויות NPM
├── tsconfig.json        → הגדרות TypeScript
├── vite.config.ts       → הגדרות Vite
├── drizzle.config.ts    → הגדרות Drizzle ORM
└── .env                 → משתני סביבה (צור חדש ב-VPS!)


🔑 משתני סביבה נדרשים (.env):
─────────────────────────────────

DATABASE_URL=postgresql://user:pass@localhost:5432/dbname
NODE_ENV=production
SESSION_SECRET=your-super-secret-random-string-min-32-chars
PORT=5000

# אופציונלי - Email
SMTP_HOST=your-smtp-host
SMTP_PORT=587
SMTP_USER=your-email@domain.com
SMTP_PASS=your-email-password
SMTP_FROM=noreply@yourdomain.com

# אופציונלי - SendGrid
SENDGRID_API_KEY=your-sendgrid-api-key


📋 נתוני כניסה ראשוניים:
────────────────────────────

👤 משתמש ראשי (Admin):
   Email: admin@example.com
   Password: admin123
   
⚠️  חשוב: שנה סיסמה מיד לאחר כניסה ראשונה!


🚀 סדר הפעולות המומלץ:
────────────────────────────

1. הכן VPS (Ubuntu 22.04 מומלץ)
2. התקן Node.js 20 + PostgreSQL 16
3. צור משתמש ומסד נתונים ב-PostgreSQL
4. שחזר את הגיבוי (database_vps_deployment_backup.sql)
5. העתק את קוד הפרויקט
6. הגדר קובץ .env
7. הרץ npm install
8. התחל עם PM2
9. הגדר Nginx (אופציונלי אך מומלץ)
10. הגדר SSL עם Let's Encrypt
11. בדוק שהכל עובד!


💡 טיפים חשובים:
─────────────────

✓ השתמש ב-PM2 לניהול תהליכים (מפעיל אוטומטית מחדש)
✓ הגדר גיבויים אוטומטיים יומיים
✓ השתמש ב-Nginx כ-reverse proxy (ביצועים + אבטחה)
✓ הפעל HTTPS עם Let's Encrypt (חינם!)
✓ הגדר firewall (UFW)
✓ עקוב אחר לוגים: pm2 logs


📞 פקודות שימושיות:
────────────────────

# בדיקת סטטוס
pm2 status
pm2 logs recruitment-system

# הפעלה מחדש
pm2 restart recruitment-system

# עדכון קוד
git pull && npm install && pm2 restart recruitment-system

# גיבוי מסד נתונים
pg_dump -U user dbname > backup_$(date +%Y%m%d).sql


🔗 משאבים:
──────────

PM2 Documentation: https://pm2.keymetrics.io/
Nginx Documentation: https://nginx.org/en/docs/
PostgreSQL Docs: https://www.postgresql.org/docs/


═══════════════════════════════════════════════════════════════
                    בהצלחה עם הפריסה! 🎉
═══════════════════════════════════════════════════════════════
