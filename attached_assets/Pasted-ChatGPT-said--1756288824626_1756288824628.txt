ChatGPT said:

מעולה, הבנתי אותך ✅
אתה עובד עם כלי בינה מלאכותית שבונה לך את המערכת, ולכן חשוב לנסח בשפה של הוראות טכניות לפיתוח ממשק — בלי בלבול עם פיצ’רים של AI בפנים.
הנה נוסח מסודר, שאתה יכול להעתיק ישירות לכלי ולתת לו לבנות את המסך:

הוראות לפיתוח מסך "מועמד חדש / כרטיס מועמד"
מבנה כללי

המסך מחולק ל־2 עמודות:

עמודה שמאלית (35%) – אזור העלאת קבצים ותצוגה מקדימה

עמודה ימנית (65%) – טופס פרטי המועמד

עמודה שמאלית – קבצים

אזור Drag & Drop עם כפתור "בחר קובץ"

רשימת קבצים שצורפו (קו״ח, תיק עבודות, מסמכים נוספים) עם אפשרויות: צפייה, הורדה, מחיקה, החלפה

תצוגה מקדימה של הקובץ הראשי (PDF/Image Viewer)

עמודה ימנית – פרטי מועמד

הטופס מחולק לכרטיסיות (Cards) מתקפלות:

כרטיסיה 1: פרטים אישיים

שם פרטי (שדה טקסט, חובה)

שם משפחה (שדה טקסט, חובה)

שם אמצעי (לא חובה)

תאריך לידה (DatePicker)

מגדר (רשימה: זכר / נקבה / לא מצוין)

ת״ז / דרכון (שדה טקסט)

אזרחות (טקסט או רשימה)

כרטיסיה 2: פרטי קשר

אימייל (שדה טקסט עם ולידציה, חובה)

טלפון 1 (שדה טקסט עם פורמט טלפון, חובה)

טלפון 2 (לא חובה)

לינקים חיצוניים: לינקדאין, אתר אישי, פורטפוליו (URL)

כרטיסיה 3: כתובת

מדינה (Dropdown)

עיר (חובה)

רחוב

מספר בית

מיקוד

כרטיסיה 4: שיוכים וסטטוס

סטטוס מועמד (Dropdown: חדש, בדיקה ראשונית, ראיון, הצעת שכר, התקבל, לא מתאים)

שיוך למשרות (Multi-select)

מקור הגעה (Dropdown: דרושים, אתר קריירה, הפניה, לינקדאין, פייסבוק, אחר)

כרטיסיה 5: העדפות

היקף משרה (מלא / חלקי / גמיש)

סוג העסקה (קבוע / פרילנס / מיקור חוץ)

ציפיות שכר (מספר + מטבע)

זמינות (תאריך)

הערות פנימיות (TextArea)

תיוגים (Tags)

כרטיסיה 6: שדות מותאמים אישית

מקום לשדות שניתן להגדיר ע״י אדמין (טקסט, מספר, תאריך, Dropdown וכו׳)

כרטיסיה 7: ציר זמן

רשימת פעולות (יצירה, עדכון שדות, העלאת קבצים, הודעות, משימות)

חלק עליון של המסך

שם המועמד (כותרת גדולה)

מזהה מועמד (ID פנימי)

כפתורי פעולה מהירים: טלפון, וואטסאפ, אימייל, SMS

סטטוס נוכחי (Dropdown)

כפתורי שמירה (שמירה, שמירה + חדש, ביטול)

דרישות טכניות

RTL מלא (עברית)

שדות חובה מסומנים בכוכבית

ולידציה בזמן אמת (אימייל, טלפון, תאריכים)

שמירה אוטומטית כטיוטה כל 30 שניות

אפשרות לעבוד גם בדסקטופ וגם במובייל (Responsive)

כל שינוי נשמר בלוג פעילות (Audit Log)

קריטריונים לסיום (Acceptance)

ניתן להעלות קובץ ולראות אותו בתצוגה מקדימה בצד שמאל

ניתן למלא/לערוך את כל השדות בטופס בצד ימין

שמירה מייצרת רשומת מועמד מלאה במערכת

סטטוס ופרטים ניתנים לעדכון בכל שלב

כל שינוי נשמר ביומן הפעולות